# D-DECISIONS-FRAMEWORK-REDESIGN Slice C — baseline cleared by full backfill.
# The migration script (migrate:decisions-backfill) emits Decision events for
# all historical IDs on every CI run. Any remaining entries here are genuinely
# pending manual triage (placeholder IDs, ambiguous resolutions).
# Target: 0 entries. Add back only if truly unresolvable.
