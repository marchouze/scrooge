{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash(git push*)",
        "hooks": [
          {
            "type": "command",
            "command": "cd \"$(git rev-parse --show-toplevel)/prototype\" && bunx biome check . --reporter=summary 2>&1 | head -20 || true"
          }
        ]
      }
    ]
  }
}
