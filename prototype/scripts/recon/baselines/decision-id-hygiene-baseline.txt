# D-DECISIONS-FRAMEWORK-REDESIGN Slice C — baseline.
#
# Sprint IDs (5): S3-S8 were pre-D-* sprint identifiers emitted by the old
# backfill-from-records.ts as CeoDecision events. They fail VALID_SLUG (no D- prefix).
# The new migration maps them to canonical slugs, but the old CeoDecision events
# persist in CI because backfill:decisions still runs before migrate:decisions-backfill.
# Fix: retire backfill-from-records.ts (follow-on cleanup, Slice D/E).
S3
S5
S6
S7
S8
#
# Prefix-shadow triage (8 items):
# These IDs are root-level siblings of namespaced slice IDs (e.g. D-RMS-PHASE-1
# vs D-RMS-PHASE-1-SLICE-3). The root IDs are canonical decisions in their own
# right; the naming collision is a design gap tracked as a Slice D item.
D-PARTY-REGISTER
D-RMS-PHASE-1
D-REPORTING-CAPABILITY-M2-M3
D-DATA-PROVENANCE-SUBSTRATE
D-FX-SALES-TRADING-FRONTEND
D-REGULATORY-READINESS-W2
D-S7-TARGETED-3-5
D-RAS
