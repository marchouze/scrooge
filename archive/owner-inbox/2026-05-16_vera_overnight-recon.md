---
agent: Vera
trigger: overnight-recon
asOf: 2026-05-16T04:43:43.079Z
decision-required: false
---

# Vera — overnight recon, 2026-05-16

Autonomous run of Vera's continuous-controls pipelines per `Team/Vera.md` operating spec § 6 (Cadence) and § 7 (Triggers). Run by the agent runtime; no human-in-the-loop.

**Headline:** PASS — 1772 assertions; 0 fail violations; 204 warn violations across 7 pipelines.

## Pipeline results

| Pipeline | OK | Asserted | Fail | Warn |
|---|---|---|---|---|
| mandate-ownership-integrity | ✓ | 209 | 0 | 0 |
| decision-event-reconciliation | ✓ | 186 | 0 | 0 |
| dashboard-derivation-reconciliation | ✓ | 208 | 0 | 12 |
| no-prose-duplication-of-canonical-facts | ✓ | 112 | 0 | 0 |
| permission-gate-default | ✓ | 443 | 0 | 41 |
| event-type-registry-coverage | ✓ | 580 | 0 | 151 |
| decision-required-event-pairing | ✓ | 34 | 0 | 0 |

## Findings

### dashboard-derivation-reconciliation

- **[warn]** `WS-REPORTING-M2-M3` — In-flight item WS-REPORTING-M2-M3 owner "Atlas + Anya + Bea" does not resolve to a direct report or known governance seat
- **[warn]** `WS-PROCEDURES-DRAFTING` — In-flight item WS-PROCEDURES-DRAFTING owner "Domain leads" does not resolve to a direct report or known governance seat
- **[warn]** `WS-INSTRUMENT-ANALYSES` — In-flight item WS-INSTRUMENT-ANALYSES owner "Mira" does not resolve to a direct report or known governance seat
- **[warn]** `WS-SUBSTRATE-BUDGET` — In-flight item WS-SUBSTRATE-BUDGET owner "Atlas + Anya" does not resolve to a direct report or known governance seat
- **[warn]** `WS-AGENT-RUNTIME-SUBSTRATE` — In-flight item WS-AGENT-RUNTIME-SUBSTRATE owner "Atlas + Anya" does not resolve to a direct report or known governance seat
- **[warn]** `workstream:atlas:substrate-gap-1` — In-flight item workstream:atlas:substrate-gap-1 owner "Atlas" does not resolve to a direct report or known governance seat
- **[warn]** `workstream:atlas:substrate-gap-2` — In-flight item workstream:atlas:substrate-gap-2 owner "Atlas" does not resolve to a direct report or known governance seat
- **[warn]** `workstream:atlas:substrate-gap-3` — In-flight item workstream:atlas:substrate-gap-3 owner "Atlas" does not resolve to a direct report or known governance seat
- **[warn]** `workstream:atlas:substrate-gap-4` — In-flight item workstream:atlas:substrate-gap-4 owner "Atlas" does not resolve to a direct report or known governance seat
- **[warn]** `workstream:atlas:substrate-gap-5` — In-flight item workstream:atlas:substrate-gap-5 owner "Atlas" does not resolve to a direct report or known governance seat
- **[warn]** `workstream:atlas:substrate-gap-6` — In-flight item workstream:atlas:substrate-gap-6 owner "Atlas" does not resolve to a direct report or known governance seat
- **[warn]** `workstream:atlas:substrate-gap-7` — In-flight item workstream:atlas:substrate-gap-7 owner "Atlas" does not resolve to a direct report or known governance seat

### permission-gate-default

- **[warn]** `actor:agent:kai:fx-pricer` — Agent actor `agent:kai:fx-pricer` has appended events without a published PermissionPolicy. Publish via `bun run identity:issue` or add to ACCEPTED_NO_POLICY_ACTORS with a citation if the carve-out is intentional. Citations: P4-SECURITY-DESIGNED-IN, ORG-CY-09, Owner Inbox/2026-05-10_senna-rashida_agent-runtime-substrate-threat-model.md (T-01), Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-031, F-011).
- **[warn]** `actor:agent:atlas:substrate-runner` — Agent actor `agent:atlas:substrate-runner` has appended events without a published PermissionPolicy. Publish via `bun run identity:issue` or add to ACCEPTED_NO_POLICY_ACTORS with a citation if the carve-out is intentional. Citations: P4-SECURITY-DESIGNED-IN, ORG-CY-09, Owner Inbox/2026-05-10_senna-rashida_agent-runtime-substrate-threat-model.md (T-01), Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-031, F-011).
- **[warn]** `actor:agent:atlas:event-trigger-bus` — Agent actor `agent:atlas:event-trigger-bus` has appended events without a published PermissionPolicy. Publish via `bun run identity:issue` or add to ACCEPTED_NO_POLICY_ACTORS with a citation if the carve-out is intentional. Citations: P4-SECURITY-DESIGNED-IN, ORG-CY-09, Owner Inbox/2026-05-10_senna-rashida_agent-runtime-substrate-threat-model.md (T-01), Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-031, F-011).
- **[warn]** `actor:agent:atlas:registry` — Agent actor `agent:atlas:registry` has appended events without a published PermissionPolicy. Publish via `bun run identity:issue` or add to ACCEPTED_NO_POLICY_ACTORS with a citation if the carve-out is intentional. Citations: P4-SECURITY-DESIGNED-IN, ORG-CY-09, Owner Inbox/2026-05-10_senna-rashida_agent-runtime-substrate-threat-model.md (T-01), Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-031, F-011).
- **[warn]** `actor:agent:atlas:permission-policy` — Agent actor `agent:atlas:permission-policy` has appended events without a published PermissionPolicy. Publish via `bun run identity:issue` or add to ACCEPTED_NO_POLICY_ACTORS with a citation if the carve-out is intentional. Citations: P4-SECURITY-DESIGNED-IN, ORG-CY-09, Owner Inbox/2026-05-10_senna-rashida_agent-runtime-substrate-threat-model.md (T-01), Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-031, F-011).
- **[warn]** `actor:agent:atlas:identity-issuer` — Agent actor `agent:atlas:identity-issuer` has appended events without a published PermissionPolicy. Publish via `bun run identity:issue` or add to ACCEPTED_NO_POLICY_ACTORS with a citation if the carve-out is intentional. Citations: P4-SECURITY-DESIGNED-IN, ORG-CY-09, Owner Inbox/2026-05-10_senna-rashida_agent-runtime-substrate-threat-model.md (T-01), Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-031, F-011).
- **[warn]** `actor:agent:kai:fx-rfq` — Agent actor `agent:kai:fx-rfq` has appended events without a published PermissionPolicy. Publish via `bun run identity:issue` or add to ACCEPTED_NO_POLICY_ACTORS with a citation if the carve-out is intentional. Citations: P4-SECURITY-DESIGNED-IN, ORG-CY-09, Owner Inbox/2026-05-10_senna-rashida_agent-runtime-substrate-threat-model.md (T-01), Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-031, F-011).
- **[warn]** `actor:agent:atlas:scheduler` — Agent actor `agent:atlas:scheduler` has appended events without a published PermissionPolicy. Publish via `bun run identity:issue` or add to ACCEPTED_NO_POLICY_ACTORS with a citation if the carve-out is intentional. Citations: P4-SECURITY-DESIGNED-IN, ORG-CY-09, Owner Inbox/2026-05-10_senna-rashida_agent-runtime-substrate-threat-model.md (T-01), Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-031, F-011).
- **[warn]** `actor:agent:vera:overnight-recon` — Agent actor `agent:vera:overnight-recon` has appended events without a published PermissionPolicy. Publish via `bun run identity:issue` or add to ACCEPTED_NO_POLICY_ACTORS with a citation if the carve-out is intentional. Citations: P4-SECURITY-DESIGNED-IN, ORG-CY-09, Owner Inbox/2026-05-10_senna-rashida_agent-runtime-substrate-threat-model.md (T-01), Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-031, F-011).
- **[warn]** `actor:agent:atlas:legacy-fanout-shadow` — Agent actor `agent:atlas:legacy-fanout-shadow` has appended events without a published PermissionPolicy. Publish via `bun run identity:issue` or add to ACCEPTED_NO_POLICY_ACTORS with a citation if the carve-out is intentional. Citations: P4-SECURITY-DESIGNED-IN, ORG-CY-09, Owner Inbox/2026-05-10_senna-rashida_agent-runtime-substrate-threat-model.md (T-01), Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-031, F-011).
- **[warn]** `actor:agent:atlas:goal-loop-runner` — Agent actor `agent:atlas:goal-loop-runner` has appended events without a published PermissionPolicy. Publish via `bun run identity:issue` or add to ACCEPTED_NO_POLICY_ACTORS with a citation if the carve-out is intentional. Citations: P4-SECURITY-DESIGNED-IN, ORG-CY-09, Owner Inbox/2026-05-10_senna-rashida_agent-runtime-substrate-threat-model.md (T-01), Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-031, F-011).
- **[warn]** `actor:agent:atlas:scheduled-trigger-consumer` — Agent actor `agent:atlas:scheduled-trigger-consumer` has appended events without a published PermissionPolicy. Publish via `bun run identity:issue` or add to ACCEPTED_NO_POLICY_ACTORS with a citation if the carve-out is intentional. Citations: P4-SECURITY-DESIGNED-IN, ORG-CY-09, Owner Inbox/2026-05-10_senna-rashida_agent-runtime-substrate-threat-model.md (T-01), Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-031, F-011).
- **[warn]** `actor:agent:vera:codebase-quality-review` — Agent actor `agent:vera:codebase-quality-review` has appended events without a published PermissionPolicy. Publish via `bun run identity:issue` or add to ACCEPTED_NO_POLICY_ACTORS with a citation if the carve-out is intentional. Citations: P4-SECURITY-DESIGNED-IN, ORG-CY-09, Owner Inbox/2026-05-10_senna-rashida_agent-runtime-substrate-threat-model.md (T-01), Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-031, F-011).
- **[warn]** `actor:agent:atlas:substrate-state` — Agent actor `agent:atlas:substrate-state` has appended events without a published PermissionPolicy. Publish via `bun run identity:issue` or add to ACCEPTED_NO_POLICY_ACTORS with a citation if the carve-out is intentional. Citations: P4-SECURITY-DESIGNED-IN, ORG-CY-09, Owner Inbox/2026-05-10_senna-rashida_agent-runtime-substrate-threat-model.md (T-01), Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-031, F-011).
- **[warn]** `actor:agent:anya:projection-refresh` — Agent actor `agent:anya:projection-refresh` has appended events without a published PermissionPolicy. Publish via `bun run identity:issue` or add to ACCEPTED_NO_POLICY_ACTORS with a citation if the carve-out is intentional. Citations: P4-SECURITY-DESIGNED-IN, ORG-CY-09, Owner Inbox/2026-05-10_senna-rashida_agent-runtime-substrate-threat-model.md (T-01), Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-031, F-011).
- **[warn]** `actor:agent:bea:accounting-readiness` — Agent actor `agent:bea:accounting-readiness` has appended events without a published PermissionPolicy. Publish via `bun run identity:issue` or add to ACCEPTED_NO_POLICY_ACTORS with a citation if the carve-out is intentional. Citations: P4-SECURITY-DESIGNED-IN, ORG-CY-09, Owner Inbox/2026-05-10_senna-rashida_agent-runtime-substrate-threat-model.md (T-01), Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-031, F-011).
- **[warn]** `actor:agent:helena:risk-appetite-watch` — Agent actor `agent:helena:risk-appetite-watch` has appended events without a published PermissionPolicy. Publish via `bun run identity:issue` or add to ACCEPTED_NO_POLICY_ACTORS with a citation if the carve-out is intentional. Citations: P4-SECURITY-DESIGNED-IN, ORG-CY-09, Owner Inbox/2026-05-10_senna-rashida_agent-runtime-substrate-threat-model.md (T-01), Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-031, F-011).
- **[warn]** `actor:agent:devon:operational-resilience-snapshot` — Agent actor `agent:devon:operational-resilience-snapshot` has appended events without a published PermissionPolicy. Publish via `bun run identity:issue` or add to ACCEPTED_NO_POLICY_ACTORS with a citation if the carve-out is intentional. Citations: P4-SECURITY-DESIGNED-IN, ORG-CY-09, Owner Inbox/2026-05-10_senna-rashida_agent-runtime-substrate-threat-model.md (T-01), Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-031, F-011).
- **[warn]** `actor:agent:camille:financial-position-snapshot` — Agent actor `agent:camille:financial-position-snapshot` has appended events without a published PermissionPolicy. Publish via `bun run identity:issue` or add to ACCEPTED_NO_POLICY_ACTORS with a citation if the carve-out is intentional. Citations: P4-SECURITY-DESIGNED-IN, ORG-CY-09, Owner Inbox/2026-05-10_senna-rashida_agent-runtime-substrate-threat-model.md (T-01), Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-031, F-011).
- **[warn]** `actor:agent:anya:projection-drift` — Agent actor `agent:anya:projection-drift` has appended events without a published PermissionPolicy. Publish via `bun run identity:issue` or add to ACCEPTED_NO_POLICY_ACTORS with a citation if the carve-out is intentional. Citations: P4-SECURITY-DESIGNED-IN, ORG-CY-09, Owner Inbox/2026-05-10_senna-rashida_agent-runtime-substrate-threat-model.md (T-01), Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-031, F-011).
- **[warn]** `actor:agent:scrooge:inbox-hygiene` — Agent actor `agent:scrooge:inbox-hygiene` has appended events without a published PermissionPolicy. Publish via `bun run identity:issue` or add to ACCEPTED_NO_POLICY_ACTORS with a citation if the carve-out is intentional. Citations: P4-SECURITY-DESIGNED-IN, ORG-CY-09, Owner Inbox/2026-05-10_senna-rashida_agent-runtime-substrate-threat-model.md (T-01), Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-031, F-011).
- **[warn]** `actor:agent:owen:governance-cycle-prep` — Agent actor `agent:owen:governance-cycle-prep` has appended events without a published PermissionPolicy. Publish via `bun run identity:issue` or add to ACCEPTED_NO_POLICY_ACTORS with a citation if the carve-out is intentional. Citations: P4-SECURITY-DESIGNED-IN, ORG-CY-09, Owner Inbox/2026-05-10_senna-rashida_agent-runtime-substrate-threat-model.md (T-01), Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-031, F-011).
- **[warn]** `actor:agent:rohan:risk-run` — Agent actor `agent:rohan:risk-run` has appended events without a published PermissionPolicy. Publish via `bun run identity:issue` or add to ACCEPTED_NO_POLICY_ACTORS with a citation if the carve-out is intentional. Citations: P4-SECURITY-DESIGNED-IN, ORG-CY-09, Owner Inbox/2026-05-10_senna-rashida_agent-runtime-substrate-threat-model.md (T-01), Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-031, F-011).
- **[warn]** `actor:agent:mira:obligations-snapshot` — Agent actor `agent:mira:obligations-snapshot` has appended events without a published PermissionPolicy. Publish via `bun run identity:issue` or add to ACCEPTED_NO_POLICY_ACTORS with a citation if the carve-out is intentional. Citations: P4-SECURITY-DESIGNED-IN, ORG-CY-09, Owner Inbox/2026-05-10_senna-rashida_agent-runtime-substrate-threat-model.md (T-01), Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-031, F-011).
- **[warn]** `actor:agent:senna:security-substrate-state` — Agent actor `agent:senna:security-substrate-state` has appended events without a published PermissionPolicy. Publish via `bun run identity:issue` or add to ACCEPTED_NO_POLICY_ACTORS with a citation if the carve-out is intentional. Citations: P4-SECURITY-DESIGNED-IN, ORG-CY-09, Owner Inbox/2026-05-10_senna-rashida_agent-runtime-substrate-threat-model.md (T-01), Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-031, F-011).
- **[warn]** `actor:agent:zara:mlro-supervision` — Agent actor `agent:zara:mlro-supervision` has appended events without a published PermissionPolicy. Publish via `bun run identity:issue` or add to ACCEPTED_NO_POLICY_ACTORS with a citation if the carve-out is intentional. Citations: P4-SECURITY-DESIGNED-IN, ORG-CY-09, Owner Inbox/2026-05-10_senna-rashida_agent-runtime-substrate-threat-model.md (T-01), Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-031, F-011).
- **[warn]** `actor:agent:thandiwe:audit-committee-prep` — Agent actor `agent:thandiwe:audit-committee-prep` has appended events without a published PermissionPolicy. Publish via `bun run identity:issue` or add to ACCEPTED_NO_POLICY_ACTORS with a citation if the carve-out is intentional. Citations: P4-SECURITY-DESIGNED-IN, ORG-CY-09, Owner Inbox/2026-05-10_senna-rashida_agent-runtime-substrate-threat-model.md (T-01), Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-031, F-011).
- **[warn]** `actor:agent:rashida:cyber-resilience-snapshot` — Agent actor `agent:rashida:cyber-resilience-snapshot` has appended events without a published PermissionPolicy. Publish via `bun run identity:issue` or add to ACCEPTED_NO_POLICY_ACTORS with a citation if the carve-out is intentional. Citations: P4-SECURITY-DESIGNED-IN, ORG-CY-09, Owner Inbox/2026-05-10_senna-rashida_agent-runtime-substrate-threat-model.md (T-01), Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-031, F-011).
- **[warn]** `actor:agent:iris:popia-controls-snapshot` — Agent actor `agent:iris:popia-controls-snapshot` has appended events without a published PermissionPolicy. Publish via `bun run identity:issue` or add to ACCEPTED_NO_POLICY_ACTORS with a citation if the carve-out is intentional. Citations: P4-SECURITY-DESIGNED-IN, ORG-CY-09, Owner Inbox/2026-05-10_senna-rashida_agent-runtime-substrate-threat-model.md (T-01), Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-031, F-011).
- **[warn]** `actor:agent:eitan:liquidity-snapshot` — Agent actor `agent:eitan:liquidity-snapshot` has appended events without a published PermissionPolicy. Publish via `bun run identity:issue` or add to ACCEPTED_NO_POLICY_ACTORS with a citation if the carve-out is intentional. Citations: P4-SECURITY-DESIGNED-IN, ORG-CY-09, Owner Inbox/2026-05-10_senna-rashida_agent-runtime-substrate-threat-model.md (T-01), Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-031, F-011).
- **[warn]** `actor:agent:saskia:markets-readiness-snapshot` — Agent actor `agent:saskia:markets-readiness-snapshot` has appended events without a published PermissionPolicy. Publish via `bun run identity:issue` or add to ACCEPTED_NO_POLICY_ACTORS with a citation if the carve-out is intentional. Citations: P4-SECURITY-DESIGNED-IN, ORG-CY-09, Owner Inbox/2026-05-10_senna-rashida_agent-runtime-substrate-threat-model.md (T-01), Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-031, F-011).
- **[warn]** `actor:agent:kai:m1-cdm-typescript-bindings` — Agent actor `agent:kai:m1-cdm-typescript-bindings` has appended events without a published PermissionPolicy. Publish via `bun run identity:issue` or add to ACCEPTED_NO_POLICY_ACTORS with a citation if the carve-out is intentional. Citations: P4-SECURITY-DESIGNED-IN, ORG-CY-09, Owner Inbox/2026-05-10_senna-rashida_agent-runtime-substrate-threat-model.md (T-01), Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-031, F-011).
- **[warn]** `actor:agent:yael:tax-readiness` — Agent actor `agent:yael:tax-readiness` has appended events without a published PermissionPolicy. Publish via `bun run identity:issue` or add to ACCEPTED_NO_POLICY_ACTORS with a citation if the carve-out is intentional. Citations: P4-SECURITY-DESIGNED-IN, ORG-CY-09, Owner Inbox/2026-05-10_senna-rashida_agent-runtime-substrate-threat-model.md (T-01), Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-031, F-011).
- **[warn]** `actor:agent:tomas:payments-readiness` — Agent actor `agent:tomas:payments-readiness` has appended events without a published PermissionPolicy. Publish via `bun run identity:issue` or add to ACCEPTED_NO_POLICY_ACTORS with a citation if the carve-out is intentional. Citations: P4-SECURITY-DESIGNED-IN, ORG-CY-09, Owner Inbox/2026-05-10_senna-rashida_agent-runtime-substrate-threat-model.md (T-01), Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-031, F-011).
- **[warn]** `actor:agent:imani:legal-readiness` — Agent actor `agent:imani:legal-readiness` has appended events without a published PermissionPolicy. Publish via `bun run identity:issue` or add to ACCEPTED_NO_POLICY_ACTORS with a citation if the carve-out is intentional. Citations: P4-SECURITY-DESIGNED-IN, ORG-CY-09, Owner Inbox/2026-05-10_senna-rashida_agent-runtime-substrate-threat-model.md (T-01), Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-031, F-011).
- **[warn]** `actor:agent:ravi:alm-readiness` — Agent actor `agent:ravi:alm-readiness` has appended events without a published PermissionPolicy. Publish via `bun run identity:issue` or add to ACCEPTED_NO_POLICY_ACTORS with a citation if the carve-out is intentional. Citations: P4-SECURITY-DESIGNED-IN, ORG-CY-09, Owner Inbox/2026-05-10_senna-rashida_agent-runtime-substrate-threat-model.md (T-01), Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-031, F-011).
- **[warn]** `actor:agent:sade:agentops-readiness` — Agent actor `agent:sade:agentops-readiness` has appended events without a published PermissionPolicy. Publish via `bun run identity:issue` or add to ACCEPTED_NO_POLICY_ACTORS with a citation if the carve-out is intentional. Citations: P4-SECURITY-DESIGNED-IN, ORG-CY-09, Owner Inbox/2026-05-10_senna-rashida_agent-runtime-substrate-threat-model.md (T-01), Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-031, F-011).
- **[warn]** `actor:agent:pax:role-research-queue` — Agent actor `agent:pax:role-research-queue` has appended events without a published PermissionPolicy. Publish via `bun run identity:issue` or add to ACCEPTED_NO_POLICY_ACTORS with a citation if the carve-out is intentional. Citations: P4-SECURITY-DESIGNED-IN, ORG-CY-09, Owner Inbox/2026-05-10_senna-rashida_agent-runtime-substrate-threat-model.md (T-01), Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-031, F-011).
- **[warn]** `actor:agent:mira:fais-horizon-scan` — Agent actor `agent:mira:fais-horizon-scan` has appended events without a published PermissionPolicy. Publish via `bun run identity:issue` or add to ACCEPTED_NO_POLICY_ACTORS with a citation if the carve-out is intentional. Citations: P4-SECURITY-DESIGNED-IN, ORG-CY-09, Owner Inbox/2026-05-10_senna-rashida_agent-runtime-substrate-threat-model.md (T-01), Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-031, F-011).
- **[warn]** `actor:agent:sade:performance-evaluator` — Agent actor `agent:sade:performance-evaluator` has appended events without a published PermissionPolicy. Publish via `bun run identity:issue` or add to ACCEPTED_NO_POLICY_ACTORS with a citation if the carve-out is intentional. Citations: P4-SECURITY-DESIGNED-IN, ORG-CY-09, Owner Inbox/2026-05-10_senna-rashida_agent-runtime-substrate-threat-model.md (T-01), Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-031, F-011).
- **[warn]** `actor:agent:ravi:ftp-curve-publish` — Agent actor `agent:ravi:ftp-curve-publish` has appended events without a published PermissionPolicy. Publish via `bun run identity:issue` or add to ACCEPTED_NO_POLICY_ACTORS with a citation if the carve-out is intentional. Citations: P4-SECURITY-DESIGNED-IN, ORG-CY-09, Owner Inbox/2026-05-10_senna-rashida_agent-runtime-substrate-threat-model.md (T-01), Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-031, F-011).

### event-type-registry-coverage

- **[warn]** `event-type:EventSchemaProposal` — Event type `EventSchemaProposal` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:IdentityPermissionChangeProposal` — Event type `IdentityPermissionChangeProposal` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:FxPositionRevalued` — Event type `FxPositionRevalued` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:FxSettlementConfirmed` — Event type `FxSettlementConfirmed` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:TradePosted` — Event type `TradePosted` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:FundingDrawn` — Event type `FundingDrawn` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:PaymentSettled` — Event type `PaymentSettled` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:AccrualBooked` — Event type `AccrualBooked` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:IFRS9ECLPublished` — Event type `IFRS9ECLPublished` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:TaxClassificationPublished` — Event type `TaxClassificationPublished` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:RestatementProposed` — Event type `RestatementProposed` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:AppetiteBreach` — Event type `AppetiteBreach` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:ModelRiskDecisionRequired` — Event type `ModelRiskDecisionRequired` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:SupervisoryLetterReceived` — Event type `SupervisoryLetterReceived` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:IcaapIlaapInputReady` — Event type `IcaapIlaapInputReady` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:RiskPolicyChangeProposal` — Event type `RiskPolicyChangeProposal` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:IncidentRaised` — Event type `IncidentRaised` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:SLOBudgetBurn` — Event type `SLOBudgetBurn` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:CapacityBreach` — Event type `CapacityBreach` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:ChangeApprovalRequested` — Event type `ChangeApprovalRequested` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:ResilienceTestResult` — Event type `ResilienceTestResult` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:CapitalEvent` — Event type `CapitalEvent` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:MaterialIFRSClassificationChange` — Event type `MaterialIFRSClassificationChange` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:RegulatorRequest` — Event type `RegulatorRequest` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:EventSchemaPublished` — Event type `EventSchemaPublished` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:PolicyChange` — Event type `PolicyChange` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:HireConfirmed` — Event type `HireConfirmed` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:MandateGapDetected` — Event type `MandateGapDetected` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:ResolutionRequired` — Event type `ResolutionRequired` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:ConflictDeclared` — Event type `ConflictDeclared` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:RelatedPartyTransactionProposed` — Event type `RelatedPartyTransactionProposed` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:WhistleblowingDisclosure` — Event type `WhistleblowingDisclosure` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:PAIARequest` — Event type `PAIARequest` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:MOIChangeProposed` — Event type `MOIChangeProposed` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:TradeBooked` — Event type `TradeBooked` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:PositionAdjusted` — Event type `PositionAdjusted` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:CollateralUpdated` — Event type `CollateralUpdated` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:LimitBreachProposed` — Event type `LimitBreachProposed` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:LimitBreachActioned` — Event type `LimitBreachActioned` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:PortfolioReclassification` — Event type `PortfolioReclassification` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:RegulatoryInstrumentUpdate` — Event type `RegulatoryInstrumentUpdate` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:SanctionsListPublished` — Event type `SanctionsListPublished` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:PepListPublished` — Event type `PepListPublished` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:ClientCandidateRegistered` — Event type `ClientCandidateRegistered` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:TransactionPosted` — Event type `TransactionPosted` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:AdverseMediaPublished` — Event type `AdverseMediaPublished` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:AlertOpened` — Event type `AlertOpened` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:MergeRequested` — Event type `MergeRequested` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:SecurityIncidentRaised` — Event type `SecurityIncidentRaised` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:KeyRotationDue` — Event type `KeyRotationDue` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:DependencyVulnDetected` — Event type `DependencyVulnDetected` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:SuspiciousAuthEvent` — Event type `SuspiciousAuthEvent` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:SBOMRequired` — Event type `SBOMRequired` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:STRCandidate` — Event type `STRCandidate` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:SanctionsHit` — Event type `SanctionsHit` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:PEPMatchExceedsThreshold` — Event type `PEPMatchExceedsThreshold` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:FAISConductBreachSuspected` — Event type `FAISConductBreachSuspected` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:RegulatorInquiry` — Event type `RegulatorInquiry` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:AuditIssueOpened` — Event type `AuditIssueOpened` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:AuditIssueClosed` — Event type `AuditIssueClosed` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:ExternalAuditorInquiry` — Event type `ExternalAuditorInquiry` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:ThreatModelExceptionRequested` — Event type `ThreatModelExceptionRequested` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:ThreatModelGateDecision` — Event type `ThreatModelGateDecision` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:KeyCeremonyScheduled` — Event type `KeyCeremonyScheduled` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:SBOMAcceptanceRequired` — Event type `SBOMAcceptanceRequired` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:VendorSecurityReview` — Event type `VendorSecurityReview` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:RegulatorCyberInquiry` — Event type `RegulatorCyberInquiry` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:PersonalInformationCompromiseSuspected` — Event type `PersonalInformationCompromiseSuspected` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:DSARReceived` — Event type `DSARReceived` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:NewProcessingPurposeProposed` — Event type `NewProcessingPurposeProposed` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:ConsentWithdrawn` — Event type `ConsentWithdrawn` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:CrossBorderTransferRequested` — Event type `CrossBorderTransferRequested` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:InformationRegulatorInquiry` — Event type `InformationRegulatorInquiry` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:IRRBBExcursion` — Event type `IRRBBExcursion` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:FXPositionBreach` — Event type `FXPositionBreach` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:LCRRatioProjection` — Event type `LCRRatioProjection` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:NSFRRatioProjection` — Event type `NSFRRatioProjection` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:CapitalActionTrigger` — Event type `CapitalActionTrigger` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:DealerMandateBreach` — Event type `DealerMandateBreach` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:SurveillanceAlert` — Event type `SurveillanceAlert` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:CurveSourceAnomaly` — Event type `CurveSourceAnomaly` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:CounterpartyEvent` — Event type `CounterpartyEvent` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:RASCalibrationChange` — Event type `RASCalibrationChange` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:LicenceGranted` — Event type `LicenceGranted` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:CEODecision` — Event type `CEODecision` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:OrderSubmitted` — Event type `OrderSubmitted` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:OrderFilled` — Event type `OrderFilled` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:PreTradeGatewayBlock` — Event type `PreTradeGatewayBlock` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:OrderRoutingAnomaly` — Event type `OrderRoutingAnomaly` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:SurveillanceFeedGap` — Event type `SurveillanceFeedGap` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:MarketDataOutage` — Event type `MarketDataOutage` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:ExchangeRuleChange` — Event type `ExchangeRuleChange` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:SARSGuidanceUpdate` — Event type `SARSGuidanceUpdate` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:IFRS9ECLChange` — Event type `IFRS9ECLChange` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:InterEntityTransactionProposed` — Event type `InterEntityTransactionProposed` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:ClientReviewTriggered` — Event type `ClientReviewTriggered` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:SettlementInstructionReceived` — Event type `SettlementInstructionReceived` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:PaymentInitiated` — Event type `PaymentInitiated` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:ReconciliationBreak` — Event type `ReconciliationBreak` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:CutOffBreach` — Event type `CutOffBreach` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:SchemeRuleChange` — Event type `SchemeRuleChange` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:CSPAttestationDue` — Event type `CSPAttestationDue` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:SanctionsHoldRaised` — Event type `SanctionsHoldRaised` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:ContractDraftRequested` — Event type `ContractDraftRequested` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:ClauseChangeProposed` — Event type `ClauseChangeProposed` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:SignatureRequested` — Event type `SignatureRequested` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:ECTAExceptionFlagged` — Event type `ECTAExceptionFlagged` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:LegalEntityChange` — Event type `LegalEntityChange` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:LoanBooked` — Event type `LoanBooked` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:DepositReceived` — Event type `DepositReceived` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:FundingDrawnDown` — Event type `FundingDrawnDown` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:SAMOSFundingShortfall` — Event type `SAMOSFundingShortfall` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:HQLACompositionDrift` — Event type `HQLACompositionDrift` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:HedgeIneffective` — Event type `HedgeIneffective` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:TokenUsageRecorded` — Event type `TokenUsageRecorded` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:AgentCapabilityChanged` — Event type `AgentCapabilityChanged` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:PersonaSpecChanged` — Event type `PersonaSpecChanged` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:Termination` — Event type `Termination` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:LeaveGranted` — Event type `LeaveGranted` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:DisciplinaryActionRequested` — Event type `DisciplinaryActionRequested` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:RoleResearchRequested` — Event type `RoleResearchRequested` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:ModelRegistered` — Event type `ModelRegistered` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:BacktestTriggered` — Event type `BacktestTriggered` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:RiskPolicyChange` — Event type `RiskPolicyChange` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:LeadCaptured` — Event type `LeadCaptured` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:SuitabilityAssessmentRequired` — Event type `SuitabilityAssessmentRequired` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:AdviceRecordRequested` — Event type `AdviceRecordRequested` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:OnboardingHandoffPending` — Event type `OnboardingHandoffPending` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:RoleBriefDelivered` — Event type `RoleBriefDelivered` is referenced by subscribesTo but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:AuditCommitteePackPrepped` — Event type `AuditCommitteePackPrepped` is referenced by eventStore.append but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:CyberResilienceSnapshot` — Event type `CyberResilienceSnapshot` is referenced by eventStore.append but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:TaxReadinessSnapshot` — Event type `TaxReadinessSnapshot` is referenced by eventStore.append but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:RoleResearchQueueSnapshot` — Event type `RoleResearchQueueSnapshot` is referenced by eventStore.append but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:ALMReadinessSnapshot` — Event type `ALMReadinessSnapshot` is referenced by eventStore.append but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:LiquiditySnapshot` — Event type `LiquiditySnapshot` is referenced by eventStore.append but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:LegalReadinessSnapshot` — Event type `LegalReadinessSnapshot` is referenced by eventStore.append but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:PaymentsReadinessSnapshot` — Event type `PaymentsReadinessSnapshot` is referenced by eventStore.append but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:RiskRunCompleted` — Event type `RiskRunCompleted` is referenced by eventStore.append but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:POPIAControlsSnapshot` — Event type `POPIAControlsSnapshot` is referenced by eventStore.append but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:OperationalResilienceSnapshot` — Event type `OperationalResilienceSnapshot` is referenced by eventStore.append but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:MarketsReadinessSnapshot` — Event type `MarketsReadinessSnapshot` is referenced by eventStore.append but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:FinancialPositionSnapshot` — Event type `FinancialPositionSnapshot` is referenced by eventStore.append but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:RiskAppetiteSnapshot` — Event type `RiskAppetiteSnapshot` is referenced by eventStore.append but has no row in EVENT_TYPE_REGISTRY (`platform/event-store/registry.ts`). Today the registry is fail-open for unknown types (build-phase tolerance per registry.ts header); subscribers will silently miss; appended events flow through the envelope-only path with no schema enforcement. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:SemanticLayerQuantityRegistered` — Registry row for `SemanticLayerQuantityRegistered` has a payloadSchema but no `makeSemanticLayerQuantityRegistered` factory in `platform/event-store/event-types.ts`. Add the factory or mark the row envelope-only. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:FxPositionRevalued` — `makeFxPositionRevalued` factory exported in `event-types.ts` but no row in EVENT_TYPE_REGISTRY. The factory's payload schema is not enforced at append-time and the type is not on Atlas's permission-policy generator surface. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:FxSettlementConfirmed` — `makeFxSettlementConfirmed` factory exported in `event-types.ts` but no row in EVENT_TYPE_REGISTRY. The factory's payload schema is not enforced at append-time and the type is not on Atlas's permission-policy generator surface. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:BalanceSheetSubstantiationCompleted` — `makeBalanceSheetSubstantiationCompleted` factory exported in `event-types.ts` but no row in EVENT_TYPE_REGISTRY. The factory's payload schema is not enforced at append-time and the type is not on Atlas's permission-policy generator surface. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:TokenUsageRecorded` — `makeTokenUsageRecorded` factory exported in `event-types.ts` but no row in EVENT_TYPE_REGISTRY. The factory's payload schema is not enforced at append-time and the type is not on Atlas's permission-policy generator surface. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:AgentEfficiencyAdvisoryIssued` — `makeAgentEfficiencyAdvisoryIssued` factory exported in `event-types.ts` but no row in EVENT_TYPE_REGISTRY. The factory's payload schema is not enforced at append-time and the type is not on Atlas's permission-policy generator surface. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:AgentPromptOptimizationApplied` — `makeAgentPromptOptimizationApplied` factory exported in `event-types.ts` but no row in EVENT_TYPE_REGISTRY. The factory's payload schema is not enforced at append-time and the type is not on Atlas's permission-policy generator surface. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).
- **[warn]** `event-type:AgentDecisionRequired` — `makeAgentDecisionRequired` factory exported in `event-types.ts` but no row in EVENT_TYPE_REGISTRY. The factory's payload schema is not enforced at append-time and the type is not on Atlas's permission-policy generator surface. Add a registry row. Citations: P1-EVENTS-AS-TRUTH, P6-SINGLE-GRAPH-DISCIPLINE, Owner Inbox/2026-05-07_atlas-kai_a0-event-schema-freeze.md, Owner Inbox/2026-05-10_vera_codebase-quality-review.md (F-032).

## Vera's narrative

**PASS.** All seven pipelines returned `ok: true` against 1,772 assertions. Every violation surfaced today is warn- or info-severity and sits inside a known, documented carve-out — no substantive control failure, and notably no empty-event-store substrate noise this run (the decision-event pipeline reconciled 186 assertions cleanly, which means the runner had a populated store).

Substantive picture underneath the headline: (a) `permission-gate-default` reports 42 agent actors appending without a published `PermissionPolicy`. This is the T-12 / Senna threat-model carve-out (`Owner Inbox/2026-05-10_senna-rashida_agent-runtime-substrate-threat-model.md`, F-031/F-011, ORG-CY-09, P4-SECURITY-DESIGNED-IN) — accepted today, but the carve-out roster is now sizeable and I want to see it shrinking, not growing. (b) `event-type-registry-coverage` reports 202 gaps (subscriber-referenced types without registry rows, factories without rows, envelope-only rows without payload schemas, and a handful of orphan factories with no consumer). These are tolerated under the registry's build-phase fail-open header (F-032, A0 schema freeze) but the surface is large enough that the freeze is doing real work — schema enforcement is *not* in force at append time. (c) `dashboard-derivation` raised 12 warns on in-flight workstream items whose owners are composites ("Atlas + Anya", "Domain leads", "Mira") that don't resolve to a single direct-report or governance seat — a labelling/data-quality issue, not a control issue. Routing: all warns; none escalated to Thandiwe today. The permission-policy carve-out and registry-coverage gaps are both on owned roadmaps (Senna T-12; Atlas/Kai A0) and will cluster into a finding if they're still at this size next cadence.

Recommendation: continue cadence; at the next run I will check that the permission-policy carve-out count and registry-coverage gap count are both moving down — if either is flat or up, I'll open a finding to Thandiwe.

## Substrate

Pipelines invoked: `mandate-ownership`, `decision-event`, `dashboard-derivation`, `prose-duplication`, `permission-gate-default`, `event-type-registry-coverage`, `decision-required-event-pairing`. Citation gate runs separately under `bun run citation-gate` / CI; future runs will wrap it here.

Events emitted: one `ReconResult` per pipeline (7); one `AuditFinding` per fail violation (0).

Routing: fail violations recommend owner `Thandiwe` (CAE) per Vera spec § 9. Warn violations are tracked but not escalated unless they cluster.
