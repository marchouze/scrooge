# D-DECISIONS-FRAMEWORK-REDESIGN Slice D — baseline updated.
#
# Sprint IDs (S3-S8) removed: backfill-from-records.ts is now a retired no-op
# stub. The old script emitted CeoDecision events with raw sprint IDs (no D-
# prefix) which failed VALID_SLUG. With the old script retired, those IDs are
# no longer emitted; the new migrate:decisions-backfill maps them to canonical
# D-* slugs. No baseline suppression needed.
#
# Prefix-shadow triage (8 items):
# These IDs are root-level siblings of namespaced slice IDs (e.g. D-RMS-PHASE-1
# vs D-RMS-PHASE-1-SLICE-3). The root IDs are canonical decisions in their own
# right; the naming collision is a design gap tracked as a Slice D item.
D-PARTY-REGISTER
D-RMS-PHASE-1
D-REPORTING-CAPABILITY-M2-M3
D-DATA-PROVENANCE-SUBSTRATE
D-FX-SALES-TRADING-FRONTEND
D-REGULATORY-READINESS-W2
D-S7-TARGETED-3-5
D-RAS
