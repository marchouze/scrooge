# D-DECISIONS-FRAMEWORK-REDESIGN Slice D — baseline updated.
#
# Group B entries (10) removed: backfill-from-records.ts is now a retired
# no-op stub (Slice D). The old script emitted terminal-only CeoDecision events
# for these IDs; without it running, the old CeoDecision events are superseded
# by the canonical Decision events from migrate:decisions-backfill which supplies
# correct symmetry (requested + terminal phase). The baseline no longer needs
# to suppress these.
#
# Triage residuals group A (4): pre-existing ghost-open / no-requested anomalies.
# These are fixed in Slice D via explicit requestDecision calls at the correct asOf.
# If they re-appear after the fix, investigate the projection's dedup logic.
#
# If new violations appear after removing Group B, add them here with a comment.

# ── Group C: Legacy pre-D-* numeric sprint IDs ──────────────────────────────
# These IDs (S3, S5, S6, S7, S8) were assigned before the D-* slug convention.
# They fail the slug validator and cannot receive synthetic `requested` openers
# via recordDecision / requestDecision. They map to canonical D-* decisions:
#   S3 → D-S7-TARGETED-3-5-OPEN-QUESTIONS
#   S5 → D-FIRST-DRY-RUN-SCENARIO
#   S6 → D-FLEET-ROLLOUT-SEQUENCING
#   S7 → D-AGENT-RUNTIME-AUTHORIZE
#   S8 → D-AGENT-AUTONOMY-OPERATIONAL
# The canonical IDs have correct symmetry; the S-prefixed projection entries
# are legacy artefacts from CeoDecision events that predate slug enforcement.
S3
S5
S6
S7
S8
